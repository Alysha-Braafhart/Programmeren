using System.Net;

namespace basicloop;

class Program
{
    static void Main(string[] args)
    {
        int[] nummers = new int[] { 42, 60, 89 };
        for (int i = 0; i < nummers.Length; i++)
        {
            double getal = nummers[i];
        }
        Console.WriteLine(nummers[i]);
    }
}
